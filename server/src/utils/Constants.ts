const Constants = {
    LOGS: {
        LOG_LEVELS: {
            DEBUG: "debug",
            INFO: "info",
            WARN: "warn",
            ERROR: "error"
        },
        DEFAULT_LOG_LEVEL: "debug"
    },
    DB: {
        MONGO: {
            CLIENT_HISTORY: {
                DB_NAME: "client-history"
            }
        }
    }
};

export default Constants;
